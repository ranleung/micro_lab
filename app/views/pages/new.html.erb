<div class = "container">
<div class = "row">
	<p>
	<%= form_for ([@user, @page]) do |f| %>
<div class = "row">
<% if @page.errors.any? %>
<div id = "error_explanation">
	<h2><%=pluralize(@page.errors.count,"error") %> prohibited this post from being saved:</h2>
	<ul>
	<% @page.errors.full_messages.each do |msg| %>
	<li><%=msg %></li>
	<% end %>
	</ul>
	</div>
	<% end %>
	<% end %>

 <%= form_for ([@user, @page]) do |f| %>
<%= f.label :name %> <br>
<%= f.text_field :name %>
<br>
<%= f.text_area :content, placeholder: "Enter Text" %>
<p>
<%=f.submit %>
</p>
</div>
<% end %>
</div>