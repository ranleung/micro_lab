<div class="container">

	<h1>About Me/Contact:</h1>





		<% if @pages != nil %>
	<% @pages.each do |page| %>
		<a href="/users/<%= @user.id %>/pages/<%= page.id %>"> <%= page.name %> </a><br>
		<% end %>
	<% end %>


	<button>
		<a href="/users/<%=@user.id%>/pages/new">Create New Page</a>
	</button>	

	<button>
		<a href="/users/<%=@user.id%>">Go Back</a>
	</button>

	<button>
		<a href="/">Go to Micro Blog Home</a>
	</button>

</div>